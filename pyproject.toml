[project]
name = "fb-rental-scanner"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
    "selenium>=4.43.0",
]
